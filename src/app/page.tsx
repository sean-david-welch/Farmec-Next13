import styles from '~/styles/Home.module.css';

const Home = () => {
    return (
        <section id={styles.home}>
            <h1>Hello world</h1>
        </section>
    );
};

export default Home;
