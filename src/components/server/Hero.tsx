import { Carousel } from '~/components/client/Carousel';
import { Typewriter } from '../client/TypeWriter';

export const Hero = () => {
    return (
        <section id="Hero">
            <Carousel />

            <Typewriter />
        </section>
    );
};
