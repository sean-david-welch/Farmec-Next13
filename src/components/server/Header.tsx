import styles from '../styles/Header.module.css';

import Navbar from '../client/Navbar';

const Header = () => {
    return (
        <header>
            <Navbar />
        </header>
    );
};

export default Header;
