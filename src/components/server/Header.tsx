import styles from '../styles/Header.module.css';

import Navbar from './Navbar';

const Header = () => {
    return (
        <header>
            <Navbar />
        </header>
    );
};

export default Header;
