import styles from '../styles/Header.module.css';

const Navbar = () => {
    return (
        <nav className={styles.navbar}>
            <h1>Farmec</h1>
        </nav>
    );
};

export default Navbar;
